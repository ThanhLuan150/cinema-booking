// Route definitions live in src/app/router.tsx (uses ROUTES from src/constants/routes.ts
// for path strings). This folder is reserved for route-level code-splitting/guards
// once features/* are migrated off the current flat <Routes> table.
export {};
