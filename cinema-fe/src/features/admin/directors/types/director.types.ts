export interface DirectorFormValues {
  full_name: string;
  avatar_url: string;
  bio: string;
  dob: string;
  nationality: string;
}

export interface AdminDirectorsState {
  showAddModal: boolean;
}
