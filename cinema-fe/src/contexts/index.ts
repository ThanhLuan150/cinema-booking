// Global state is managed via Redux Toolkit (see src/app/store.ts).
// This folder is reserved for narrowly-scoped React Context providers
// (e.g. a theme or feature-flag context) that don't belong in the global store.
export {};
